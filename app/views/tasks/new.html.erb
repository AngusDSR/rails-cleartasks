Add a new note
  <%= simple_form_for(@task) do |form| %>
    <div>
      <p>CAR</p>
      <%= form.label :content %>
      <%= form.text_field :content, required: true %>
    </div>
    <%= form.submit "Create Task" %>
  <% end %>
<hr>
