<h1>Tasks: Home</h1>
Add a new note
  <%= simple_form_for(@new_task) do |form| %>
    <div>
      <%= form.label :content %>
      <%= form.text_field :content, required: true %>
    </div>
    <%= form.submit "Create Task" %>
  <% end %>
<hr>1

<h2>Notes</h2>
<% @tasks.each do |task| %>
  <div>
    <%= task.content %>
    <hr>
  </div>
<% end %>\
